#include<stdio.h>
int main()
{
    int a,b,gcd;
    printf("enter the numbers");
    scanf("%d %d",&a,&b);
    printf("Gcd of %d and %d is %d",a,b.gcd(a,b));
    return 0;
}

int gcd(int a,int b)
{
    if(b!=0)
    {
        return gcd(b,a%b);
    
    }
    else
    {
        return a;
    }
}
